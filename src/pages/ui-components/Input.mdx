---
title: Input
type: documentation
componentName: Input
navigation: Components
---

import {Input, Field, Box, Icon, Text, Link, Heading} from "rimble-ui"
import DoDont from "../../components/documentation/DoDont";

# Input
Use `Input` to build forms for your interface. This component provides a styled version of HTML `<input>`.

**Import component**

```lang=jsx
import { Input } from 'rimble-ui';
```

## Examples
Different ways to use the `Input` component in your product. *Note: these examples use the [`Field`](/ui-components/Field) component to render associated labels.*

**Placeholder**

Adds placeholder text to suggest the type of input expected.

```js is=react-live
<Input type="text" required="true" placeholder="e.g. My hodl wallet" />
```
**Disabled input**

```js is=react-live
<Input type="text" placeholder="e.g. My hodl wallet" required="true" disabled />
```

**Optional input**

Renders "(Optional)" next to the label.

```js is=react-live
<Input type="text" placeholder="e.g. My hodl wallet" />
```


**Text input**

Brings up a standard keyboard.

```js is=react-live
<Form.Input type="text" required="true" placeholder="e.g. 0xAc03BB73b6a9e108530AFf4Df5077c2B3D481e5A" />
```

**Email input**

Give you an @ on your keyboard.

```js is=react-live
<Form.Input type="email" required="true" placeholder="you@example.com />
```

**Password input**

Masks any input.

```js is=react-live
<Form.Input type="password" required="true" />
```

**Number input**

Opens the number page of your keyboard.

```js is=react-live
<Form.Input type="number" required="true" placeholder="e.g. 123" />
```


**Search input**

Allows users to quickly remove a search term.

```js is=react-live
<Form.Input type="search" required="true" placeholder="e.g. Ethereum" />
```

**Telephone input**

Brings up the telephone keyboard.

```js is=react-live
<Form.Input type="tel" required="true" placeholder="e.g. +445575242519" />
```

**URL input**

Bring up a url keyboard.

```js is=react-live
<Form.Input type="url" required="true" placeholder="e.g. rimble.consensys.design" />
```

## Design guidelines

Some best practices for using `Input` in your product.

**Don't use placeholders instead of labels**

Placeholders disappear as the user enters the field. This makes reviewing form entries difficult as you lose the explanation of the expected input.

<DoDont>
  {{
    positive: (
      <Box>
        <Field label="Public address">
          <Input
            type="text"
            width="100%"
            required="true"
            placeholder="e.g. 0xAc03BB73b6a9e108530AFf4Df5077c2B3D481e5A"
          />
        </Field>
      </Box>
    ),
    negative: (
      <Box>
        <Field>
          <Input
            type="text"
            width="100%"
            required="true"
            placeholder="Public address"
          />
        </Field>
      </Box>
    ),
  }}
</DoDont>

## Related components

- [`Field`](/ui-components/Field)
- [`Form validation`](/ui-components/Form)
