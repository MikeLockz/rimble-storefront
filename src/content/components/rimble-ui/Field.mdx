---
title: Field
componentName: Field
description: The Field component provides a convenient way to add a label to an Input component.
---

The `Field` component provides a convenient way to add a label to an [Input](/components/rimble-ui/Input) component.

**Import component**

```lang=jsx
import { Field } from 'rimble-ui';
```

## Example

```js is=react-live
<Field label="ENS address">
  <Input type="text" required={true} placeholder="e.g. aardvark.eth" />
</Field>
```

## Props

<PropsTable propMetaData={props.propMetaData} />

** Styled System Props **

`Field` uses `COMMON`, `LAYOUT`, `POSITION`, `FLEXBOX`, `BORDERS` groups of [Styled System props](/components/rimble-ui/system-props).
