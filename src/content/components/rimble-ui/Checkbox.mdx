---
title: Checkbox
componentName: Checkbox
description: Use the Checkbox component when users can choose 0 or more than 1 options from a list.
image: "/images/Checkbox.png"
---

Use the `Checkbox` component when users can choose 0 or more than 1 options from a list. Checkbox maps to a checkbox input and a label.

**Import component**

```lang=jsx
import { Checkbox } from 'rimble-ui';
```

## Examples

Different ways to use the `Checkbox` component

**Default checkbox**

```js is=react-live
<Checkbox label="Checkbox default" required={true} />
```

**Checked**

```js is=react-live
<Checkbox defaultChecked label="Checkbox checked" required={true} />
```

**Disabled**

```js is=react-live
<Checkbox disabled label="Checkbox disabled" required={true} />
```

**Checked and disabled**

```js is=react-live
<Checkbox
  checked
  disabled
  label="Checkbox checked disabled"
  my={2}
  required={true}
/>
```

## Props

<PropsTable propMetaData={props.propMetaData} />

** Styled System Props **

`Checkbox` uses `COMMON`, `LAYOUT`, `POSITION`, `FLEXBOX`, `BORDERS` groups of [Styled System props](/components/rimble-ui/system-props).

## Design guidelines

Some best practices for using `Checkbox` in your product.

**Always use with a label**

<DoDont>
  {{
    positive: (
      <Box>
        <Text
          fontSize={1}
          mb={3}
        >
          Use a label to provide users with instructions on what to select.
        </Text>
        <hr color="#f4f4f4" />
        <Field
          label="Choose any currencies you'd like to track"
          mt={3}
        >
          <Checkbox label="Ether (ETH)" required={true} my={2} />
          <Checkbox label="Bitcoin (BTC)" required={true} my={2} />
          <Checkbox label="Ethereum Classic (ETC)" required={true} my={2} />
          <Checkbox label="Litecoin (LTC)" required={true} my={2} />
        </Field>
      </Box>
    ),
    negative: (
      <Box>
        <Text
          fontSize={1}
          mb={3}
        >
          Don't just provide a user with unlabelled choices.
        </Text>
        <hr color="#f4f4f4" />
        <Checkbox label="Ether (ETH)" required={true} mt={3} mb={2} />
        <Checkbox label="Bitcoin (BTC)" required={true} my={2} />
        <Checkbox label="Ethereum Classic (ETC)" required={true} my={2} />
        <Checkbox label="Litecoin (LTC)" required={true} my={2} />
      </Box>
    )
  }}
</DoDont>

**Only use when users can make multiple selections**

<DoDont caption="test">
  {{
    positive: (
      <Box>
        <Text
          fontSize={1}
          mb={3}
        >
          Checkboxes are for times when your users can choose multiple options or no options.
        </Text>
        <hr color="#f4f4f4" />
        <Field
          mt={3}
          label="Choose any currencies you'd like to track"
        >
          <Checkbox label="Ether (ETH)" required={true} my={2} />
          <Checkbox label="Bitcoin (BTC)" required={true} my={2} />
          <Checkbox label="Ethereum Classic (ETC)" required={true} my={2} />
          <Checkbox label="Litecoin (LTC)" required={true} my={2} />
        </Field>
      </Box>
    ),
    negative: (
      <Box>
        <Text
          fontSize={1}
          mb={3}
        >
          Checkboxes are not suitable for binary decisions like Yes/No.
        </Text>
        <hr color="#f4f4f4"/>
        <Field
          mt={3}
          label="Would you like alerts when the price changes?"
        >
          <Checkbox label="Yes" required={true} my={2} />
          <Checkbox label="No" required={true} my={2} />
        </Field>
      </Box>
    )
  }}
</DoDont>

## Related components

- [Select](/components/rimble-ui/Select) - when users can only choose one option from a long list
- [Radio buttons](/components/rimble-ui/Radio) – When users can only choose one option or the choices are binary like yes/no
