---
title: Textarea
componentName: Textarea
---

import { Textarea } from "rimble-ui";
import PropsTable from "../../../components/propstable";

# Textarea

The `Textarea` component provides a styled version of the HTML `<textarea>`.

**Import component**

```lang=jsx
import { Textarea } from 'rimble-ui';
```

## Example

```js is=react-live
<Textarea defaultValue="Start typing..." rows={4} />
```

## Props

<PropsTable propMetaData={props.propMetaData} />

** Styled System Props **

`Textarea` uses `COMMON`, `LAYOUT`, `POSITION`, `FLEXBOX`, `BORDERS` groups of [Styed System props](/components/rimble-ui/system-props).
