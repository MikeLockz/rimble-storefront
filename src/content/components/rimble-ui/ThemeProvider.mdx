---
title: Theme Provider
componentName: ThemeProvider
description: ThemeProvider allows for overriding the default component styles holistically from a single theme definition.
---

import PropsTable from "../../../components/documentation/PropsTable.js";
import { ThemeProvider } from "rimble-ui";

ThemeProvider allows for overriding the default component styles holistically from a single theme definition.

Rimble components use [styled-system](https://github.com/jxnblk/styled-system) to support theme-based variables.

**Import component**

```lang=jsx
import { ThemeProvider, theme } from 'styled-components'
```

## Example

**Default theme**

```js is=react-live
<ThemeProvider theme={theme}>
  <Button>Button</Button>
</ThemeProvider>
```

**Custom theme**

```js is=react-live
<ThemeProvider theme={myTheme}>
  <Button>Button</Button>
</ThemeProvider>
```

## Props

<PropsTable propMetaData={props.propMetaData} />
