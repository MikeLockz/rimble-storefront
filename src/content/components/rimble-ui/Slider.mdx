---
title: Slider
componentName: Slider
description: The Slider component provides a styled slider control for the HTML <input type="range">.
---

The `Slider` component provides a styled slider control for the HTML `<input type="range">`.

**Import component**

```lang=jsx
import { Slider } from 'rimble-ui';
```

## Example

```js is=react-live
<Slider min={"0"} max={"100"} step={"10"} />
```

## Props

<PropsTable propMetaData={props.propMetaData} />

** Styled System Props **

`Box` uses `COMMON`, `LAYOUT`, `POSITION`, `FLEXBOX`, `BORDERS` groups of [Styled System props](/components/rimble-ui/system-props).
