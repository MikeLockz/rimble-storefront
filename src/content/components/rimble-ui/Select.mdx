---
title: Select
componentName: Select
description: Use the Select component when you want your users to select an option from a list. Select provides a styled version of the HTML <select>. This component is specifically useful for long lists you wouldn't want to display using Radio buttons like currencies or states.
image: "/images/Select.png"
---

Use the `Select` component when you want your users to select an option from a list. Select provides a styled version of the HTML `<select>`. This component is specifically useful for long lists you wouldn't want to display using [Radio](/components/rimble-ui/Radio) buttons like currencies or states.

**Import component**

```lang=jsx
import { Select } from 'rimble-ui';
```

## Example

```js is=react-live
<Field label="Choose your currency">
  <Select
    required={true}
    options={[
      { value: "eth", label: "Ethereum" },
      { value: "btc", label: "Bitcoin" },
      { value: "gno", label: "Gnosis" },
      { value: "gnt", label: "Golem" },
      { value: "rep", label: "Augur" }
    ]}
  />
</Field>
```

**Options as children**

```js is=react-live
<Field label={"Choose your currency"}>
  <Select>
    <optgroup label="Fiat">
      <option value="usd">US dollar</option>
      <option value="gbp">Great Britain pound</option>
      <option value="brl" disabled>
        Brazilian real
      </option>
    </optgroup>
    <optgroup label="Crypto">
      <option value="btc">Bitcoin</option>
      <option value="eth">Ethereum</option>
      <option value="ltc">Litecoin</option>
    </optgroup>
  </Select>
</Field>
```

## Props

<PropsTable propMetaData={props.propMetaData} />

** Styled System Props **

`Select` uses `COMMON`, `LAYOUT`, `POSITION`, `FLEXBOX`, `BORDERS` groups of [Styled System props](/components/rimble-ui/system-props).

## Design guidelines

Some best practices for using `Select` in your product.

**It's better to show your options**

<DoDont>
  {{
    positive: {
      caption: (<>Use a Select component if there are lots of choices that might clutter an interface. Or use a <Link href="/components/rimble-ui/Radio" title="To Radio component" target="_blank">Radio</Link> component instead.</>),
      code: (
        <Box>
          <Box mt={3}>
            <Select
              options={[
                { value: "eth", label: "Ethereum" },
                { value: "btc", label: "Bitcoin" },
                { value: "gno", label: "Gnosis" },
                { value: "gnt", label: "Golem" },
                { value: "rep", label: "Augur" }
              ]}
            />
          </Box>
          <Text my={4}>OR</Text>
          <Radio label="ETH - Ether" my={2} />
          <Radio label="FIAT e.g. USD" my={2} />
        </Box>
      )
    },
    negative: {
      caption: "Don't use a Select component when there are only a few options as it'll be quicker and simpler to just display the available choices.",
      code: (
        <Box mt={3}>
          <Select
            options={[
              { value: "eth", label: "Ethereum" },
              { value: "fiat", label: "USD" }
            ]}
          />
      </Box>
      )
    }
  }}
</DoDont>

## Related components

- [Radio](/components/rimble-ui/Radio) - When users can only choose one option or the choices are binary like yes/no
- [Checkbox](/components/rimble-ui/Checkbox) – When users can choose 0 or multiple options from a list
