---
title: Connection request error
description: Grab the code for a ready-made modal template that makes sure a user wanted to cancel a connection request.
image: "/images/connection-request-error-template.png"
---

A connection request error modal should be displayed if the user cancels the connection request, especially if they were prompted to connect after trying to transact. "Connect" should re-start the flow from where they cancelled without having to go through any earlier steps/screens.

```js is=react-live hideCode
// import { Heading, Text, Modal, Flex, Box, Button, Card } from "rimble-ui";

<Card p={0} borderRadius={1} mb={4}>
  <Box height="4px" bg="danger" borderRadius={["1rem 1rem 0 0"]} />
  <Flex
    justifyContent="space-between"
    alignItems="center"
    borderBottom={1}
    borderColor="near-white"
    p={[3, 4]}
    pb={3}
  >
    <Icon name="Warning" color="danger" aria-label="Warning" />
    <Heading textAlign={["left", "center"]} as="h1" fontSize={[2, 3]} px={[3, 0]}>
      Cancel connection?
    </Heading>
    <Link>
      <Icon
        name="Close"
        color="moon-gray"
        aria-label="Close and cancel connection"
      />
    </Link>
  </Flex>
  <Box p={[3, 4]}>
    <Text>
      To buy this Cryptokitty, you need to connect your Ethereum account.
    </Text>
  </Box>
  <Flex
    p={[3, 4]}
    borderTop={1}
    borderColor="near-white"
    justifyContent="flex-end"
    flexDirection={["column", "row"]}
    alignItems="center"
  >
    <Button.Outline
      variant="danger"
      mr={[0, 3]}
      mb={[2, 0]}
      width={["100%", "auto"]}
    >
      Cancel connection
    </Button.Outline>
    <Button width={["100%", "auto"]}>Connect</Button>
  </Flex>
</Card>
```

---

## Template anatomy

<Image
  width="100%"
  src="/images/connection-request-error-anatomy.png"
  alt="An annotated image calling out the different elements of the template"
  mt={3}
  borderTop={1}
  borderWidth={3}
  borderColor="primary-light"
/>
<Heading as="h4"
  color="dark-gray"
  alignItem="flex-end"
  mt={4}
>
  1. Title
</Heading>
<Text
  color="mid-gray"
  mb={3}
>
  Makes sure the user is aware of their action.
</Text>
<Heading as="h4"
  color="dark-gray">
  2. Body
</Heading>
<Text
  color="mid-gray"
  mb={3}
>
  Reiterates the impact of cancelling the connection request in relation to the task (connecting).
</Text>
<Heading as="h4"
  color="dark-gray">
  3. Cancel connection button
</Heading>
<Text
  color="mid-gray"
  mb={3}
>
  Provides a way for the user to confrim their connection cancellation.
</Text>
<Heading as="h4" color="dark-gray">
  4. Connect button
</Heading>
<Text
  color="mid-gray"
  mb={4}
>
  Re-prompts the connection request so the user doesn't have to go through any earlier steps.
</Text>

---

## See demo

<TemplateConnectionDemo />

---

<GuideNextPrevious
  previousTitle={"To template for Signature request modal"}
  previousUrl={"/templates/signature-request"}
  previousGuideName={"Signature request modal"}
  nextTitle={"To template for Signature request error"}
  nextUrl={"/templates/signature-request-error"}
  nextGuideName={"Signature request error"}
/>
